version: "3"

services:
  dash-redis-srv:
    image: redis:6.2.3
    container_name: dash-redis-srv
    restart: unless-stopped
    command: redis-server --save 3600 1
    ports:
      - "6379:6379"
    volumes:
      - redis-data-vol:/data
    networks:
      - redis-net
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

  dash-import-app:
    build: dash-import-app/.
    image: dash-import-app-img
    container_name: dash-import-app
    restart: unless-stopped
    volumes:
      - dashboard-conf-vol:/data/dashboard-conf-vol
    networks:
      - redis-net
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

volumes:
  dashboard-conf-vol:
    external: true
  redis-data-vol:
    external: true

networks:
  redis-net:
    external: true

